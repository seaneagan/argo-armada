
name: armada

# See examples directory.
dag: null
groups: null

task_template:
  release_prefix: armada
  container:
    image: alpine:latest
    command: [echo]
    # TODO: this is the real implementation, once armada supports it
    # image: quay.io/airshipit/armada:master-ubuntu_xenial
    # command: [sh, -c]
